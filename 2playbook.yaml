- name: install and run nginx
  hosts: web
  become: true
  tasks:
    - name: install nginx
      ansible.builtin.package:
        name: nginx
        state: present
    - name: run nginx
      ansible.buoltin.service:
        name: nginx
        state: started
        enabled: yes


