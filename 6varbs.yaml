- name: vriables preference
  hosts: local
  connection: local
  vars:
   NAME: "Ansible from play"

  vars_files:
   - course.yaml
  vars_prompt:
   - name: NAME
     prompt: please enter name
     private: false
  tasks:
    - name: print name
      vars:
       NAME: "Ansible from tasks"
       ansible.builtin.debug:
        msg: "Hi {{ NAME }}"